The app does not work, issues includes:

## Overal

- 'Energy' and the option 'Include in total' with help text: 'Should this device count towards the total home's energy usage?', is missing from device settings.

## Local (LAN) mode

- During install, it shows 'offline' behind the 'Local (LAN)' option, not sure if this is good or bad, I can imagine when the IP address and port has been given in app settings, it makes sense but in case no IP address and port has been given, this looks weird. Either remove or only show this option in case basd on given IP address and port the device seems to be offline.
- During installtion I cannot enter the port, let me add the port which has '10081' as the default value.
- After the device is added and I go into 'Settings', I can't change the 'Port'.
- After the device has been added, it shows 'Device Unavailable'.

## Local + Cloud mode

- The translate icon 'NL'/'EN' is staying in 'NL' and not switching to 'EN', what is expected when the translations are in Dutch.
- In the screen where I need to enter the IP address I want to update the 'note', see the html here: "<div class="tip" id="t-tip">Find the IP in your router under 'connected devices'.<br>Look for <strong>DTS-…</strong>, <strong>DTUBI-…</strong> or <strong>HiBox</strong>.<br>Tip: give the gateway a <strong>fixed IP address</strong> (DHCP reservation) in your router so the local connection keeps working.</div>", also make sure to support this in NL and EN!
- During installtion I cannot enter the port, let me add the port which has '10081' as the default value.
- After entering the username (email address) and password, I can't continue, nothing happens when I press the 'Log in' button.

## Cloud only mode

- After given username (email address) and password, and I click 'Log in', it shows 'Login failed. Please check your credentials.'. Note that my crednetials are correct, I am able to login with them on the website: https://global.hoymiles.com/website/login without issues.

## Changes

- In app settings ('Configure') I want to be able to enter all details, currently I can only enter the CLOUD API URL. Options I would like to manage on app level includes:
  - IP Address of HiOne station
  - Port of HiOne station
  - S-Miles cloud username
  - S-Miles cloud password

## Instructions

- Analyze all issues and change requests, check neapi API documentation at hoymiles.com and fix all settings.
- Keep same names accross all locations in the app and devices created with the app.
- Propose any other changes which makes sense to add to the application.